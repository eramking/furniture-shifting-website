// src/components/PlaceholderLogo.js
import React from "react";

export const PlaceholderLogo = ({ className }) => {
  return <div className={`placeholder-logo ${className}`}>Placeholder Logo</div>;
};